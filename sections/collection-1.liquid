{{ 'collection-1.css' | asset_url | stylesheet_tag }}
{% render 'section-style' %}

{% style %}
  #shopify-section-{{ section.id }} {
    --heading-color-1: {{ section.settings.heading_color | color_extract: 'red' }}, {{ section.settings.heading_color | color_extract: 'green' }}, {{ section.settings.heading_color | color_extract: 'blue' }};
    --color-body: {{ section.settings.text_color | color_extract: 'red' }}, {{ section.settings.text_color | color_extract: 'green' }}, {{ section.settings.text_color | color_extract: 'blue' }};
    --ap-nextap-buttonprev-background: var(--color-body);
    --ap-nextap-buttonprev-color: var(--section-background);
    --section-collage-column: {{ section.settings.number_column }};
    --border-color: {{ section.settings.border_color }};
  }
  {% for block in section.blocks %}
    #block-{{ block.id }}-{{ forloop.index }} {
      --heading-color: {{ block.settings.heading_color | color_extract: 'red' }}, {{ block.settings.heading_color | color_extract: 'green' }}, {{ block.settings.heading_color | color_extract: 'blue' }};
      --section-block-overlay: {{ block.settings.overlay | color_extract: 'red' }}, {{ block.settings.overlay | color_extract: 'green' }}, {{ block.settings.overlay | color_extract: 'blue' }};
      --section-block-overlay-opacity: {{ block.settings.overlay_opacity | divided_by: 10.0 }};
    }
  {% endfor %}

  .collection-1 .section__header {
    padding-left: var(--container-distance);
    padding-right: var(--container-distance);
  }

  #shopify-section-{{ section.id }} .list-collections__item-list {
    gap: calc({{ section.settings.distance_col }}px + {{ section.settings.distance_col }}px) 0;
    margin: 0 calc(-1 * {{ section.settings.distance_col }}px);
  }
  #shopify-section-{{ section.id }} .list-collections__item {
    padding: 0 {{ section.settings.distance_col }}px;
  }

{% endstyle %}

<section class="section section--flush collection-1">
  <div class="section__color-wrapper">
    <div class="vertical-breather">
      {% if section.settings.heading != blank %}
        <header class="section__header mb-50">
          {% if section.settings.sub_heading != blank %}
            <p class="heading sub-heading mb-20">{{ section.settings.sub_heading }}</p>
          {% endif %}
          <h3 class="heading h{{ section.settings.heading_size_range }}">{{ section.settings.heading }}
          </h3>
          {% if section.settings.tab_description != blank %}
          <div class="heading-description">
            {{ section.settings.tab_description }}
          </div>
          {% endif %}
          {% if section.settings.btn_text != blank %}
          <div class="btn-more mb-60 mt-20">
            <a href="{{ section.settings.btn_link }}" class="button {{ section.settings.style_btn }}">
              {{ section.settings.btn_text }}
            </a>
          </div>
          {% endif %}
        </header>
      {% endif %}
      
      <ap-collectionlist reveal-on-scroll="" class="list-collections list-collections--collage ap-object-loaded">
        <div class="{%- if section.settings.fullwidth -%}container-fluid{%- else -%}container{%- endif -%}">
          <div class="list-collections__item-list collection-1-content">
            {% for block in section.blocks %}
              {% liquid
                assign _link = block.settings.collection.url
                if block.settings.link != ''
                  assign _link = block.settings.link
                endif
              %}
              <div id="block-{{ block.id }}-{{ forloop.index }}" class="list-collections__item">  
                <a href="{{ _link }}" class="list-collections__item-link has-overlay image-zoom collection-item-{{ forloop.index }}">
                  <div class="list-collections__item-image-wrapper">
                    {% if block.settings.featured_image != blank %}
                      <img class="list-collections__item-image" reveal="" loading="lazy" 
                        sizes="(max-width: 740px) 80vw, (max-width: 999px) 60vw, 425px"
                        alt="" 
                        src="{{ block.settings.featured_image | image_url: width: block.settings.featured_image.width }}" 
                        data-srcset="{%- if block.settings.featured_image.width >= 195 -%}{{ block.settings.featured_image | image_url: width: 195, crop: 'center' }} 195w,{%- endif -%}
                          {%- if block.settings.featured_image.width >= 330 -%}{{ block.settings.featured_image | image_url: width: 330, crop: 'center' }} 330w,{%- endif -%}
                          {%- if block.settings.featured_image.width >= 535 -%}{{ block.settings.featured_image | image_url: width: 535, crop: 'center' }} 535w,{%- endif -%}
                          {%- if block.settings.featured_image.width >= 750 -%}{{ block.settings.featured_image | image_url: width: 750, crop: 'center' }} 750w,{%- endif -%}
                          {%- if block.settings.featured_image.width >= 1000 -%}{{ block.settings.featured_image | image_url: width: 1000, crop: 'center' }} 1000w,{%- endif -%}
                          {%- if block.settings.featured_image.width >= 1500 -%}{{ block.settings.featured_image | image_url: width: 1500 }} 1500w,{%- endif -%}
                          {%- if block.settings.featured_image.width >= 3000 -%}{{ block.settings.featured_image | image_url: width: 3000, crop: 'center' }} 3000w,{%- endif -%}
                          {{ block.settings.featured_image | image_url }} {{ block.settings.featured_image.width }}w"
                        src="{{ block.settings.featured_image | image_url: width: 1500 }}"
                        height="{{ block.settings.featured_image.height }}"
                        width="{{ block.settings.featured_image.width }}">
                    {% else %}
                      <div class="list-collections__item-image">
                        {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
                      </div>
                    {% endif %}
                  </div>
                  <div class="list-collections__item-info text-container">

                    {% if block.settings.heading != blank %}
                    <h4 class="heading h{{ section.settings.heading_size_range_item }}" reveal="">
                      <span>
                        {{- block.settings.heading -}}
                      </span>
                    </h4>
                    {% else %}
                    <h4 class="heading h{{ section.settings.heading_size_range_item }}" reveal="">
                      {{- block.settings.collection.title | escape -}}
                    </h4>
                    {% endif %}
                    {% if section.settings.show_count %}
                      <span class="heading count" reveal="">{{ block.settings.collection.products_count | append: ' ' | append: ' products' }}</span>
                    {% endif %}
                    {% if block.settings.button1_label != blank %}
                    <div class="collection-btn">
                        <a href="{{ block.settings.link1 }}" reveal="" class="button {{ section.settings.style_btn_item }}">
                            {{ block.settings.button1_label }}
                        </a>
                    </div>
                    {% endif %}

                  </div>
                </a>
              </div>
            {% endfor %}
          </div>
        </div>
      </ap-collectionlist>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "t:sections.collection-1.name",
  "tag": "section",
  "class": "section shopify-section--collection-1",
  "max_blocks": 15,
  "settings": [
    {
      "type": "checkbox",
      "id": "fullwidth",
      "label": "Full width",
      "default": false
    },
    {
      "type": "color",
      "id": "background-color",
      "default": "#ffffff",
      "label": "t:sections.all.background-color.label"
    },
    {
      "type": "image_picker",
      "id": "background_image",
      "label": "t:sections.all.background_image.label"
    },
    {
      "type": "color",
      "id": "heading_color",
      "default": "#282828",
      "label": "t:sections.all.heading_color.label"
    },
    {
      "type": "color",
      "id": "text_color",
      "default": "#282828",
      "label": "t:sections.all.text_color.label"
    },
    {
      "type": "color",
      "id": "border_color",
      "label": "Border button"
    },
    {
      "type": "range",
      "id": "number_column",
      "min": 1,
      "max": 5,
      "step": 1,
      "default": 2,
      "label": "t:sections.all.number_column.label"
    },
    {
      "type": "liquid",
      "id": "sub_heading",
      "default": "Sub heading",
      "label": "Sub Heading"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "t:sections.all.heading.label"
    },
    {
      "type": "range",
      "id": "heading_size_range",
      "min": 1,
      "max": 6,
      "step": 1,
      "default": 1,
      "label": "t:sections.all.heading_size_range.label"
    },
    {
      "type": "textarea",
      "id": "tab_description",
      "label": "t:sections.all.tab_description.label"
    },
    {
      "type": "text",
      "id": "btn_text",
      "label": "Button Text"
    },
    {
      "type": "url",
      "id": "btn_link",
      "label": "Link"
    },
    {
      "type": "select",
      "id": "style_btn",
      "label": "Style Button",
      "default": "btn-base",
      "options": [
        {
          "value": "btn-base",
          "label": "Button Base"
        },
        {
          "value": "btn-outline",
          "label": "Button Out Line"
        },
        {
          "value": "btn-line",
          "label": "Button Line"
        },
        {
          "value": "btn-line-black",
          "label": "Button Line Black"
        }
      ]
    },
    {
      "type": "checkbox",
      "id": "show_count",
      "label": "Show Count"
    },
    {
      "type": "text",
      "id": "margin_desktop",
      "label": "t:sections.all.margin_desktop.label",
      "info": "t:sections.all.margin_desktop.info"
    },
    {
      "type": "text",
      "id": "margin_mobile",
      "label": "t:sections.all.margin_mobile.label",
      "info": "t:sections.all.margin_mobile.info"
    },
    {
      "type": "text",
      "id": "padding_desktop",
      "label": "t:sections.all.padding_desktop.label",
      "info": "t:sections.all.padding_desktop.info"
    },
    {
      "type": "text",
      "id": "padding_mobile",
      "label": "t:sections.all.padding_mobile.label",
      "info": "t:sections.all.padding_mobile.info"
    },
    {
      "type": "header",
      "content": "Settings Items"
    },
    {
      "type": "text",
      "id": "distance_col",
      "label": "Distance Column",
      "default": "15"
    },
    {
      "type": "range",
      "id": "heading_size_range_item",
      "min": 1,
      "max": 6,
      "step": 1,
      "default": 4,
      "label": "t:sections.all.heading_size_range.label"
    },
    {
      "type": "select",
      "id": "style_btn_item",
      "label": "Style Button",
      "default": "btn-base",
      "options": [
        {
          "value": "btn-base",
          "label": "Button Base"
        },
        {
          "value": "btn-outline",
          "label": "Button Out Line"
        },
        {
          "value": "btn-line",
          "label": "Button Line"
        },
        {
          "value": "btn-line-black",
          "label": "Button Line Black"
        }
      ]
    }
  ],
  "blocks": [
    {
      "type": "featured_collection",
      "name": "Collection",
      "settings": [
        {
          "type": "collection",
          "id": "collection",
          "label": "t:sections.all.collection.label"
        },
        {
          "type": "image_picker",
          "id": "featured_image",
          "label": "Image Collection"
        },
        {
          "type": "color",
          "id": "heading_color",
          "default": "#282828",
          "label": "t:sections.all.heading_color.label"
        },
        {
          "type": "liquid",
          "id": "heading",
          "label": "t:sections.all.heading.label"
        },
        {
          "type": "text",
          "id": "button1_label",
          "label": "Button"
        },
        {
          "type": "url",
          "id": "link1",
          "label": "Link"
        },
        {
          "type": "color",
          "id": "overlay",
          "default": "#000000",
          "label": "t:sections.all.overlay.label"
        },
        {
          "type": "range",
          "id": "overlay_opacity",
          "min": 0,
          "max": 10,
          "step": 1,
          "default": 1,
          "label": "t:sections.all.overlay_opacity.label"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Collections 1",
      "blocks": [
        {
          "type": "featured_collection"
        },
        {
          "type": "featured_collection"
        },
        {
          "type": "featured_collection"
        }
      ]
    }
  ]
}
{% endschema %}
